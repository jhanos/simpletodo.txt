<?xml version="1.0" encoding="utf-8"?>
<!-- Root FrameLayout — drawer panel overlays main content -->
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/rootLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Main content -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <ListView
            android:id="@+id/listView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:divider="#33FFFFFF"
            android:dividerHeight="0.5dp"
            android:scrollbarStyle="outsideOverlay" />

    </LinearLayout>

    <TextView
        android:id="@+id/emptyView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:text="@string/no_tasks"
        android:textSize="16sp"
        android:visibility="gone" />

    <!-- FAB: plain circle button, bottom-right -->
    <Button
        android:id="@+id/fab"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:layout_gravity="bottom|end"
        android:layout_margin="16dp"
        android:text="+"
        android:textSize="24sp"
        android:textColor="@android:color/white"
        android:background="@drawable/bg_fab" />

    <!-- Scrim: semi-transparent overlay behind drawer; closes drawer on tap -->
    <View
        android:id="@+id/drawerScrim"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#88000000"
        android:visibility="gone" />

    <!-- Navigation drawer panel — slides in from the left -->
    <ListView
        android:id="@+id/drawerList"
        android:layout_width="240dp"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:background="@color/colorPrimary"
        android:divider="#33FFFFFF"
        android:dividerHeight="0.5dp"
        android:choiceMode="singleChoice"
        android:elevation="8dp"
        android:visibility="gone" />

</FrameLayout>
